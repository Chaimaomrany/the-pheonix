{% extends 'base-back.html.twig' %}
{% block body %}


    {% for p in Produit %}

<img src="{{ qr_code_path('./generate.php"') }}" />

{# You can specify the builder via the second parameter #}
<img src="{{ qr_code_data_uri('http://localhost:6463/phpmyadmin/index.php?route=/sql&db=web&table=produit&pos=0') }}" />
    {% endfor %}
{% endblock %}